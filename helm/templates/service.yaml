apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-app-go.fullname" . }}
  labels:
    {{- include "demo-app-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "demo-app-go.selectorLabels" . | nindent 4 }}
